{
  "name": "resume-creator",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "copy": "copy db.json db-running.json",
    "serve": "json-server-auth db-running.json --port=8000",
    "start": "npm run copy && npm run serve"
  }
}
